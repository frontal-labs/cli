// Generated by frontal types — do not edit.
// Source: frontal.jsonc

type FrontalSdk = import("@frontal-labs/sdk").Frontal;

export type FrontalEnvName = "dev" | "staging" | "prod";
export type FrontalServiceName = "agents" | "ai" | "graph";
export type FrontalKnownService = "agents" | "ai" | "audit" | "auth" | "billing" | "blob" | "connectors" | "data" | "datasets" | "events" | "governance" | "graph" | "integrations" | "lineage" | "observability" | "ontology" | "pipelines" | "sandbox" | "schedules" | "webhooks" | "workers" | "workflows";

/** Plain configuration values from `vars` in frontal.jsonc. */
export interface FrontalVars {
  LOG_LEVEL: string;
  REGION: string;
}

/** Secrets that must be present in the environment (`secrets.required`). */
export interface FrontalSecrets {
  FRONTAL_API_KEY: string;
  OPENAI_API_KEY: string;
}

/** Environment variables a Frontal app can rely on at runtime. */
export interface FrontalEnv extends FrontalVars, FrontalSecrets {
  FRONTAL_ENV?: FrontalEnvName;
  FRONTAL_API_URL?: string;
}

/** Services enabled in frontal.jsonc and their SDK clients. */
export interface FrontalServices {
  agents: { remote: false; client: FrontalSdk["agents"] };
  ai: { remote: true; client: FrontalSdk["ai"] };
  graph: { remote: false; client: FrontalSdk["graph"] };
}

/** The validated frontal.jsonc for this project. */
export interface FrontalProject {
  name: "fixture-app";
  env: FrontalEnvName;
  apiUrl: string;
  services: FrontalServices;
  vars: FrontalVars;
  secrets: { required: (keyof FrontalSecrets)[] };
}

declare global {
  namespace NodeJS {
    // biome-ignore lint/suspicious/noEmptyInterface: augmentation
    interface ProcessEnv extends FrontalEnv {}
  }
}
