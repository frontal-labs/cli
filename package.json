{
  "name": "frontal-cli",
  "version": "0.1.2",
  "description": "Frontal CLI",
  "homepage": "https://github.com/frontal-labs/cli",
  "bugs": "https://github.com/frontal-labs/cli/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frontal-labs/cli.git"
  },
  "license": "Apache-2.0",
  "author": "Frontal Labs",
  "sideEffects": false,
  "type": "module",
  "bin": {
    "frontal": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run generate:openapi-ops && bun run validate:bindings && tsup && chmod +x dist/index.js",
    "build:all": "bun run build && bun run build:binary",
    "build:binaries": "bun scripts/build-binaries.ts",
    "build:binary": "bun scripts/build-binaries.ts --current",
    "changeset": "changeset",
    "check": "bun run generate:openapi-ops && bun run validate:bindings && bun scripts/generate-schema.ts --check && ultracite check",
    "clean": "rm -rf dist",
    "dev": "FRONTAL_CLI_VERSION=$(node -p \"require('./package.json').version\") bun run bin/frontal.ts",
    "fix": "ultracite fix",
    "format": "biome format --write src/ tests/",
    "generate:openapi-ops": "node ./scripts/generate-openapi-operations.mjs",
    "generate:schema": "bun scripts/generate-schema.ts",
    "lint": "biome lint src/ tests/",
    "prepare": "husky",
    "prepublishOnly": "bun run build",
    "release": "bun run build && changeset publish",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:readme": "bun run build && bun scripts/run-readme-blocks.ts",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "validate:bindings": "node ./scripts/validate-command-bindings.mjs",
    "version-packages": "changeset version"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@frontal-labs/core": "1.0.4",
    "@frontal-labs/sdk": "1.0.4",
    "chalk": "6.0.0",
    "cli-table3": "^0.6.5",
    "commander": "^15.0.0",
    "ora": "^9.4.0",
    "yaml": "^2.7.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@changesets/cli": "3.0.2",
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@frontal-labs/testing": "1.0.4",
    "@types/bun": "^1.3.10",
    "@types/node": "26.5.1",
    "@vitest/coverage-v8": "5.0.0",
    "ajv": "8.20.0",
    "husky": "9.1.7",
    "tsup": "^8.0.0",
    "typescript": "7.0.2",
    "ultracite": "7.11.1",
    "vite-tsconfig-paths": "6.1.1",
    "vitest": "5.0.0"
  },
  "packageManager": "bun@1.3.8",
  "engines": {
    "bun": ">=1.3.8",
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
