# Frontal CLI Bun configuration
# Builds go through tsup (npm bundle) and `bun build --compile` (single binary);
# tests run with vitest. See package.json scripts.

[install]
# Install exact versions (the SDK is pinned)
exact = true
